{
    "name": "express_server",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "start": "set NODE_ENV=dev && nodemon app",
        "start-dev": "set NODE_ENV=dev && nodemon app",
        "start-prod": "set NODE_ENV=prod && node app"
    },
    "dependencies": {
        "cors": "^2.8.5",
        "dotenv": "^8.2.0",
        "express": "~4.16.1",
        "helmet": "^4.3.1",
        "http-errors": "~1.6.3",
        "morgan": "~1.9.1",
        "rotating-file-stream": "^2.1.3"
    },
    "devDependencies": {
        "nodemon": "^2.0.7"
    }
}
